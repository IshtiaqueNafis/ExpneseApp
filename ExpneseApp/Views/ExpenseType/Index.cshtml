@model IEnumerable<ExpneseApp.Models.ExpenseType>
<div class="container">
    <div class="row pt-4">
        
        <div class="col-6">
            <h2 class="text-primary">Showed all Expenses</h2>
        </div>
        <div class="col-6">
            
            <a asp-controller="ExpenseType" asp-action="Create" class="btn btn-success">Create New item</a>
           


        </div>

    </div>

</div>
    
<table class="table table-dark">
    <thead>
    <tr>
        <th scope="col">ID</th>
        <th scope="col">Expense Type</th>
    </tr>
    </thead>
    <tbody>


    @foreach (var expenseType in Model)

    {
        <tr>
            <td width="30%">@expenseType.Id</td>
            <td width="30%">@expenseType.ExpenseCategory</td>
            <td width="40%">
                <a asp-controller="ExpenseType" asp-action="Update" asp-route-Id="@expenseType.Id" class="btn btn-primary" mx-1>Update</a>
                                           
                <a asp-controller="ExpenseType" asp-action="Delete" asp-route-Id="@expenseType.Id" class="btn btn-danger" mx-1>Delete</a>
                <!-- in the asp-route-Id="expense.Id" is there to get the id  -->
            </td>
            
        </tr>

    }



    </tbody>
</table>